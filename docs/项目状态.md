# 项目状态

最后更新：2026-06-18 18:39

## 项目类型和技术栈

- 类型：可部署 Web 应用。
- 前端：React + TypeScript + Vite。
- 数据层：Drizzle ORM。
- 本地数据库：SQLite。
- 云端目标：Cloudflare Workers + D1 + R2。
- 备选部署：Docker 云服务器。
- UI 方向：朴素后台风格，优先功能完整。

## 当前目标

先做一个可靠的 MVP 骨架，跑通真实直播核心链路：

```text
创建场次
  -> 定榜
  -> 生成/确认名单
  -> 对局锁牌
  -> 存票/取票/现刷
  -> 结算
  -> 游客查看公开存票榜
```

## 当前状态

| 模块 | 状态 | 当前说明 | 下一步 |
| --- | --- | --- | --- |
| 项目规划文档 | 已完成初版 | 需求、数据模型、页面流程、MVP、技术方案、数据库草案、项目创建方案已整理 | 随功能变化持续更新 |
| 项目骨架 | 已创建 | React + Vite + TypeScript 基础结构已创建 | 补基础路由和模块目录 |
| 数据库 schema | 初版完成 | Drizzle schema 和首版 SQL 迁移已创建 | 跑迁移并结合实现修正 |
| Cloudflare 适配 | 占位完成 | Worker 入口、D1/R2 配置占位已创建 | 后续配置真实 Cloudflare 资源 |
| 账号登录 | 未实现 | 已规划管理员/主播登录 | 实现管理员初始化和登录 |
| 主播/粉丝资料 | 未实现 | 已规划字段和状态 | 实现 CRUD |
| 直播场次 | 未实现 | 已规划场次状态和结算 | 实现场次创建和状态流转 |
| 定榜 | 未实现 | 已规划定榜七候选和定榜五顺延 | 实现定榜录入和名单生成 |
| 锁牌 | 未实现 | 已规划互斥、淘汰释放占用 | 实现核心规则和页面 |
| 存票 | 未实现 | 已规划流水、修正、作废、结算 | 实现票务流水和余额计算 |
| 截图 | 未实现 | 已规划本地/R2 存储适配 | 实现本地上传，后接 R2 |
| 游客公开榜 | 未实现 | 已规划公开字段和模块开关 | 实现公开存票榜 |
| 操作日志 | 未实现 | 已规划关键操作日志 | 接入关键操作 |
| Git 版本管理 | 已初始化 | 新项目需要 Git 管理版本、变更记录和提交规则 | 执行首次提交 |
| 主分支迁移 | 已完成本地替换 | 旧仓库 `main` 已替换为新项目内容，旧项目已备份到 `old-mvp-main` | 提交并推送 `main` |

## 当前已验证

- npm 代理已按 Clash Verge `7897` 端口修正。
- pip 代理已按 Clash Verge `7897` 端口修正。
- npm 查询包版本成功。
- pip 查询包版本成功。
- Git 文档和常用命令已补充到 `CHANGELOG.md`。
- `git init` 已完成。
- `npm run typecheck` 已通过。
- `npm run build` 已通过。
- `git status --short --branch` 已确认首次提交范围正常，构建产物和依赖目录未进入待提交列表。
- `AGENTS.md` 已加入 Git 提交规则。
- 旧项目当前状态已保存到远程分支 `old-mvp-main`。
- 旧仓库 `main` 工作区已替换为新项目内容。

## 当前未验证

- `npm run dev`
- Drizzle 迁移生成和执行。
- Cloudflare Worker 本地运行。
- D1/R2 真实部署。

## 当前维护原则

- 功能完整优先于视觉复杂度。
- 票务和锁牌规则优先保证正确性和可追溯。
- 所有敏感操作保留日志。
- 公开页只展示公开字段。
- 每次重要变更更新 `CHANGELOG.md`。
- 每次遇到配置、依赖、部署、数据问题，更新 `docs/问题汇总与解决.md`。
